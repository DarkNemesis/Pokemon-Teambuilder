#include <set>
#include <vector>
#include <array>
#include <utility>
#include <fstream>
#include <algorithm>
#include <unordered_map>
#include <map>
#include <iterator>
#include <random>
#include <iostream>
using namespace std;

/*vector<int> mUser[NO_OF_USER];

fstream fout("Output.txt", fstream::out);

void fileWrite(string filename)
{
	fstream fout(filename);
	for (int i = 0; i < NO_OF_USER; i++)
	{
		for (auto j = mUser[i].begin(); j != mUser[i].end(); j++)
		{
			fout << i + 1 << " " << *j << endl;
		}
	}
}

#define SIZE 100
void fileRead(string filename)
{
	vector<array<int, 6>> mTeams(SIZE);
	
	fstream fin(filename);
	for (int i = 0; i < SIZE; i++)
	{		
		for (int j = 0; j < 6; j++)
			fin >> mTeams[i][j];
	}		
}*/
